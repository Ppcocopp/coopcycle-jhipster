<template>
  <div class="row justify-content-center">
    <div class="col-8">
      <div v-if="pro">
        <h2 class="jh-entity-heading" data-cy="proDetailsHeading"><span v-text="$t('blogApp.pro.detail.title')">Pro</span> {{ pro.id }}</h2>
        <dl class="row jh-entity-details">
          <dt>
            <span v-text="$t('blogApp.pro.description')">Description</span>
          </dt>
          <dd>
            <span>{{ pro.description }}</span>
          </dd>
          <dt>
            <span v-text="$t('blogApp.pro.mail')">Mail</span>
          </dt>
          <dd>
            {{ pro.mail ? pro.mail.id : '' }}
          </dd>
        </dl>
        <button type="submit" v-on:click.prevent="previousState()" class="btn btn-info" data-cy="entityDetailsBackButton">
          <font-awesome-icon icon="arrow-left"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.back')"> Back</span>
        </button>
        <router-link v-if="pro.id" :to="{ name: 'ProEdit', params: { proId: pro.id } }" custom v-slot="{ navigate }">
          <button @click="navigate" class="btn btn-primary">
            <font-awesome-icon icon="pencil-alt"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.edit')"> Edit</span>
          </button>
        </router-link>
      </div>
    </div>
  </div>
</template>

<script lang="ts" src="./pro-details.component.ts"></script>
